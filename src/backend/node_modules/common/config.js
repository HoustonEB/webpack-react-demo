var path = require('path');
var _ = require('lodash');

var config = {
    root: path.normalize(__dirname + '/../../../../'),
    path: {}
};

config.path.src = path.resolve(config.root + '/src/');
config.path.frontend = path.resolve(config.path.src + '/frontend/');
config.path.backend = path.resolve(config.path.src + '/backend/');
config.path.dest = path.resolve(config.root + '/dest/');
config.path.projectNodeModules = path.resolve(config.path.root + '/node_modules/');
config.path.frontendNodeModules = path.resolve(config.path.frontend + '/node_modules/');
config.path.backendNodeModules = path.resolve(config.path.backend + '/node_modules/');

// 编译
config.path.prod = path.resolve(config.path.dest + '/prod/');
config.path.prodPages = path.resolve(config.path.prod + '/pages/');
config.path.prodDll = path.resolve(config.path.prod + '/dll/');
config.path.prodDist = path.resolve(config.path.prod + '/dist/');
config.path.prodExtra = path.resolve(config.path.prod + '/extra/');

config.path.dev = path.resolve(config.path.dest + '/dev/');
config.path.devPages = path.resolve(config.path.dev + '/pages/');
config.path.devDll = path.resolve(config.path.dev + '/dll/');
config.path.devDist = path.resolve(config.path.dev + '/dist/');
config.path.devExtra = path.resolve(config.path.dev + '/extra/');

// 编译模板
config.path.webpackConfig = path.resolve(config.path.backend + '/webpack-config/');
config.path.webpackTemplates = path.resolve(config.path.webpackConfig + '/templates/');

module.exports = config;